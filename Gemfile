source "https://rubygems.org"

# Mirrors the gem set GitHub Pages uses to build the site, so local
# previews match production. Run: bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins
